<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>OpenDisplay</string>
	<key>CFBundleDisplayName</key>
	<string>OpenDisplay</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<!-- Menu-bar agent: no Dock icon (PRD UX-001). -->
	<key>LSUIElement</key>
	<true/>
	<!-- Clock Mode (Issue 30): optional one-shot location lookup to auto-fill the sunrise/sunset
	     latitude & longitude. Only requested when the user taps "Use current location"; a manual
	     entry is always available instead. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>OpenDisplay uses your location once to calculate local sunrise, noon, and sunset times for Clock Mode brightness schedules. You can also enter your coordinates manually.</string>
	<!-- Sparkle 2 in-app updates. The feed is the newest release's appcast.xml asset, reached through
	     GitHub's stable "latest" redirect, so the URL never has to be re-pointed at a new tag.
	     SUPublicEDKey is the EdDSA public key every downloaded update is verified against; its private
	     half lives only in the maintainer's login keychain and signs the appcast at release time (see
	     scripts/sparkle-setup.md). Automatic checking/downloading is NOT declared here: both are user
	     settings the app writes into Sparkle at launch, so settings.json stays the one place a
	     preference lives. -->
	<key>SUFeedURL</key>
	<string>https://github.com/aquitaine/OpenDisplay/releases/latest/download/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>G9aQchK8B0eqrXhKwKWat4SRCR/XboPdgpmfuHrBnuE=</string>
	<!-- opendisplay:// URL-scheme automation (Issue 4). Routes onto the same safety-checked,
	     audited CommandGateway as the CLI and App Intents; only non-destructive recovery
	     commands auto-run, arrangement-altering ones require in-app confirmation. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>opendisplay</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
